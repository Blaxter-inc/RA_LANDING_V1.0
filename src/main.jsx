import React from "react";
import ReactDOM from "react-dom";
import App from "./App.jsx"; // If you're using JSX files, keep the .jsx extension
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
